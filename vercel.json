{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm install"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app.js" }
  ],
  "dependencies": {
    "mysql2": "^3.11.0"
  }
}
